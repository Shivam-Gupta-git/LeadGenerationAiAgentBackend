import { authenticate, protect, authorizeRoles, restrictTo, tenantGuard } from './auth.js';

export {
  authenticate,
  protect,
  authorizeRoles,
  restrictTo,
  tenantGuard
};

export default {
  authenticate,
  protect,
  authorizeRoles,
  restrictTo,
  tenantGuard
};
